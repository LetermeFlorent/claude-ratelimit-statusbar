{
  "name": "claude-ratelimit-statusbar",
  "displayName": "Claude Rate Limit Status Bar",
  "description": "Affiche les limites d'usage Claude (fenetres 5h et 7 jours) avec une vraie barre de progression coloree dans la status bar. Source API /usage fiable + repli cache CLI.",
  "version": "0.2.4",
  "publisher": "letermeflorent",
  "license": "MIT",
  "icon": "icon.png",
  "repository": { "type": "git", "url": "https://github.com/LetermeFlorent/claude-ratelimit-statusbar.git" },
  "homepage": "https://github.com/LetermeFlorent/claude-ratelimit-statusbar#readme",
  "bugs": { "url": "https://github.com/LetermeFlorent/claude-ratelimit-statusbar/issues" },
  "keywords": ["claude", "anthropic", "rate limit", "usage", "status bar"],
  "engines": { "vscode": "^1.75.0" },
  "categories": ["Other"],
  "activationEvents": ["onStartupFinished"],
  "main": "./extension.js",
  "extensionKind": ["ui"],
  "contributes": {
    "commands": [
      { "command": "claudeRate.refresh", "title": "Claude Rate: Rafraichir" }
    ],
    "icons": {
      "cs-bar-full": {
        "description": "Barre pleine (JetBrains Mono CS)",
        "default": { "fontPath": "./bar.woff", "fontCharacter": "\\E000" }
      },
      "cs-bar-empty": {
        "description": "Barre vide (JetBrains Mono CS)",
        "default": { "fontPath": "./bar.woff", "fontCharacter": "\\E001" }
      }
    },
    "configuration": {
      "title": "Claude Rate Limit",
      "properties": {
        "claudeRate.refreshSeconds": { "type": "number", "default": 5, "description": "Intervalle de rafraichissement de l'affichage (secondes)." },
        "claudeRate.apiIntervalSeconds": { "type": "number", "default": 180, "description": "Intervalle minimum entre deux appels API usage (secondes). Plus grand = moins de risque de 429." },
        "claudeRate.staleSeconds": { "type": "number", "default": 300, "description": "Age au-dela duquel les donnees sont grisees (secondes)." },
        "claudeRate.barWidth": { "type": "number", "default": 8, "description": "Largeur de la barre (cellules)." },
        "claudeRate.apiFallback": { "type": "boolean", "default": true, "description": "Interroger l'API /usage (>=1/min max) comme source fiable ; repli sur le cache CLI si desactive." },
        "claudeRate.cachePath": { "type": "string", "default": "", "description": "Chemin du cache cs-rate-cache.json (vide = %TEMP%\\cs-rate-cache.json)." }
      }
    }
  }
}
